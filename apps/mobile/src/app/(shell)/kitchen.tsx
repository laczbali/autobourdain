import { Placeholder } from '@/components/shell';

export default function Kitchen() {
  return (
    <Placeholder note="What is in the fridge, freezer and pantry, and the shopping list the gaps produce." />
  );
}
