import { Placeholder } from '@/components/shell';

export default function Settings() {
  return (
    <Placeholder note="Preferences, setup and appearance. The next block fills this one in." />
  );
}
